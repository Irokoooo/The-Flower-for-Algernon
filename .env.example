# Local only: put the replacement credential in .env.local (ignored by Git).
# Never use VITE_ prefixes for server-side generation credentials.
OPENAI_BASE_URL=https://api.openai-next.com/v1
OPENAI_API_KEY=replace-with-local-key
